applications:
  - name: agile-dev-starter
    memory: 1024M
    disk_quota: 1024M
    buildpacks:
      - java_buildpack
    stack: cflinuxfs3
#    services:
    env:
      JBP_CONFIG_OPEN_JDK_JRE: '{ jre: { version: 11.+ }}'
#      JBP_CONFIG_CONTAINER_CERTIFICATE_TRUST_STORE: '{enabled: true}'
      SPRING_PROFILES_INCLUDE: cloud
